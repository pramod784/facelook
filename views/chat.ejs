
<!DOCTYPE html>
<html>
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Facelook Chatbot</title>
</head>
<body class="container py-4">
    <h3>Chat with Facelook Bot</h3>

    <div class="chat-window mb-3" id="chatWindow">
        <% messages.forEach(m => { %>
            <div class="<%= m.sender === 'user' ? 'chat-bubble-user ms-auto' : 'chat-bubble-bot' %>">
                <%= m.text %>
            </div>
        <% }) %>
    </div>

    <form action="/chat" method="POST">
        <input type="text" class="form-control mb-3" name="message" placeholder="Type a message...">
        <button class="btn btn-primary w-100">Send</button>
    </form>
</body>
</html>
